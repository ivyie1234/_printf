#include "main.h"

/**
 * print_37 - Prints % followed by a new line.
 * @y: An integer parameter (not used in this case).
 * @x: A character parameter (not used in this case).
 *
 * Return: 0 on success.
 */
int print_37(int y, char x)
{
	(void)y;
	(void)x;

	put_char('%');
	put_char('\n');
	return (0);
}
