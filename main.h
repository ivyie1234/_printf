#ifndef MAIN_H
#define MAIN_H

#include<stdarg.h>
#include <stdio.h>
#include <stdlib.h>
int put_char(char c);
//int printf_char(va_list, val);
int _putchar(char c);
int _printf(const char *format, ...);

#endif
